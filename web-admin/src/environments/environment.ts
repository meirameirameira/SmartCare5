/**
 * Endereco da API REST do Smart HAS consumida pelo painel.
 *
 * E a mesma API usada pelo aplicativo Flutter: painel e app nao compartilham
 * codigo, mas compartilham o contrato.
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080',
};
